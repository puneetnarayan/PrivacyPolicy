# The real places-india.db you build/receive (~97MB) should NOT be
# committed on top of the tracked placeholder — too large for comfortable
# git history (close to GitHub's 100MB hard limit). Replace the tracked
# placeholder file locally (git will just see it as locally modified) but
# don't add/commit that change. See README.md, "Optional supplementary
# database".
node_modules/
